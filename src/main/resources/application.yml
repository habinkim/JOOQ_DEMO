spring:
  application:
    name: sakila_app
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sakila
    username: root
    password: 1234

logging:
  level:
    org:
      jooq:
        tools:
          LoggerListener: DEBUG
